# RoBERTa with the JSNLI dataset

This directory describes how to execute the experiments of the following models:

- RoBERTa<sub>jsnli</sub> Cond A
- RoBERTa<sub>jsnli</sub> Cond B

Check the following directories:

- `ConditionA/`
- `ConditionB/`
